export const title = "When NOT to use serverless"

export const description = "Learn which cases the serverless approach is not recommended"

export const image = "/no.jpg"

# When NOT to use serverless
